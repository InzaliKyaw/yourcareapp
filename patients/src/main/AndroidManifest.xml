<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yourcare">

    <application
        android:name=".PatientApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourCare">
        <activity android:name=".activities.MainActivity">

        </activity>
        <activity android:name=".activities.RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity">


        </activity>
        <activity android:name=".activities.CaseSummaryActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:parentActivityName=".activities.MainActivity">

        </activity>
        <activity android:name=".activities.CaseSummaryTwoActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:parentActivityName=".activities.CaseSummaryActivity">

        </activity>
        <activity android:name=".activities.CheckoutActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:parentActivityName=".activities.ChatActivity">

        </activity>
        <activity android:name=".activities.ConfirmCaseSummaryActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:parentActivityName=".activities.CaseSummaryTwoActivity">

        </activity>
        <activity android:name=".activities.ChatActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:parentActivityName=".activities.MainActivity">

        </activity>
    </application>

</manifest>